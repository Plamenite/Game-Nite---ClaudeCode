# Loaded automatically when the server runs in production.
# This file is committed: NO secrets here. Real values are set in the
# hosting provider's environment settings and override these.

# Never accept development guest tokens in production.
ALLOW_GUEST_TOKENS=false

# Set in the hosting provider's environment settings:
# SUPABASE_URL=https://<project>.supabase.co
